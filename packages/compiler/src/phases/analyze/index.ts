/**
 * Usage Analysis Module
 *
 * Phase 1: Identification
 * Generic infrastructure for extracting usage facts from source code
 * Use-case-specific types (AnalyzedContext, *Usage) are exported from config modules
 */

export { analyze } from './analyze';
