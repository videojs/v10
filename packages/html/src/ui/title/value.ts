import { UIElement } from '../ui-element';

/** Displays the resolved text inside a `<media-title>` root. */
export class TitleValueElement extends UIElement {
  static readonly tagName = 'media-title-value';
}
