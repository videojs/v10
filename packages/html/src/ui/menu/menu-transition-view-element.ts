import { MediaElement } from '../media-element';

/** Structural binding for one child menu destination view. */
export class MenuTransitionViewElement extends MediaElement {
  static readonly tagName = 'media-menu-transition-view';
}
