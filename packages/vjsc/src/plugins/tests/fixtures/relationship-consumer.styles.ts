import { styles } from 'vjsc/styles';

export default styles({
  file: 'components/consumer.css',
  layer: 'fixture.components',
  rules: {
    root: {
      className: 'fixture-consumer',
      utilities: 'peer-data-open/control:hidden',
    },
  },
});
