import { styles } from 'vjsc/styles';

export default styles({
  file: 'components/owner.css',
  layer: 'fixture.components',
  rules: {
    root: {
      className: 'fixture-owner',
      utilities: 'peer/control',
    },
  },
});
