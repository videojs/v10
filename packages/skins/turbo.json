{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build", "^build:cdn", "generate", "@videojs/html#generate", "@videojs/react#generate"],
      "outputs": ["dist/**", "types/**", "*.tsbuildinfo", ".netlify/**"]
    }
  }
}
