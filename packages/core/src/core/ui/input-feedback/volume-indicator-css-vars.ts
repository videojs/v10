/** CSS custom property names for volume indicator visual state. */
export const VolumeIndicatorCSSVars = {
  /** Current volume level used to animate the fill bar. */
  fill: '--media-volume-fill',
} as const;
