/** CSS custom property names for input feedback visual state. */
export const InputFeedbackCSSVars = {
  /** Volume progress percentage. */
  volumePercentage: '--media-volume-percentage',
} as const;
