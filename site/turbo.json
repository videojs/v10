{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "api-docs:generate": {
      "dependsOn": ["^build"],
      "outputs": [
        "src/content/generated-component-reference/**",
        "src/content/generated-util-reference/**",
        "src/content/generated-feature-reference/**",
        "src/content/generated-media-reference/**",
        "src/content/generated-preset-reference/**"
      ]
    },
    "ejected-skins": {
      "dependsOn": ["^build"],
      "outputs": ["src/content/ejected-skins.json"]
    },
    "cdn-manifest": {
      "dependsOn": ["^build:cdn"],
      "outputs": ["src/content/cdn-media.json"]
    },
    "build": {
      "dependsOn": ["$TURBO_EXTENDS$", "api-docs:generate", "ejected-skins", "cdn-manifest"],
      "env": [
        "OAUTH_CLIENT_ID",
        "OAUTH_CLIENT_SECRET",
        "OAUTH_REDIRECT_URI",
        "OAUTH_URL",
        "MUX_API_URL",
        "SESSION_COOKIE_PASSWORD",
        "SENTRY_AUTH_TOKEN"
      ]
    },
    "dev": {
      "dependsOn": ["$TURBO_EXTENDS$", "api-docs:generate", "ejected-skins", "cdn-manifest"],
      "env": [
        "OAUTH_CLIENT_ID",
        "OAUTH_CLIENT_SECRET",
        "OAUTH_REDIRECT_URI",
        "OAUTH_URL",
        "MUX_API_URL",
        "SESSION_COOKIE_PASSWORD",
        "SENTRY_AUTH_TOKEN"
      ]
    }
  }
}
