---
import type { DocStability } from '@/types/docs';

type Props = {
  stability: DocStability;
};

const { stability } = Astro.props;
const labels = {
  experimental: 'Experimental',
} satisfies Record<DocStability, string>;

// A dev server that cached frontmatter before a rename can hand us a value the map doesn't know; show it raw.
const label = labels[stability] ?? stability;
---

<span
  class="inline-flex h-4.5 shrink-0 items-center rounded-md corner-squircle bg-orange/12 px-1.5 font-mono text-p4 leading-none text-orange select-none dark:bg-orange/15"
>
  {label}
</span>
