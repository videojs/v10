---

import type { HTMLTag, Polymorphic } from 'astro/types';
import { clsx } from 'clsx';
import { twMerge } from 'tailwind-merge';

type Props<Tag extends HTMLTag = 'h3'> = Polymorphic<{ as: Tag }> & {
  maxWidth?: boolean;
  class?: string;
};

const { as: Tag = 'h3', maxWidth = true, class: className, ...props } = Astro.props;
---

<Tag
  class={twMerge(clsx('text-h5 @lg:text-h4 @3xl:text-h3 mb-6 mt-12', maxWidth && 'max-w-3xl mx-auto'), className)}
  {...props}
>
  <slot />
</Tag>
