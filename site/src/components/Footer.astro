---
import { ArrowUpRight } from 'lucide-react';
import { DISCORD_INVITE_URL, GITHUB_REPO_URL } from '@/consts';
import Discord from './icons/Discord';
import GitHub from './icons/GitHub';
import { LogoMonoVersion } from './icons/Logo';
import { ThemeToggle } from './ThemeToggle';

type NavLink = {
  href: string;
  label: string;
  external?: boolean;
};
const navLinks: NavLink[] = [
  { href: '/docs', label: 'Docs' },
  { href: '/blog', label: 'Blog' },
  { href: '/privacy', label: 'Privacy' },
];

type Props = {
  class?: string;
};
const { class: className } = Astro.props;

const navLinkBase =
  'hover:bg-(--color-dark-manila) transition-colors flex items-center px-0 md:px-5 py-2 rounded-sm font-display-extended uppercase font-bold';
---

<footer
  class:list={[
    'w-full max-w-6xl mx-auto md:border-t border-faded-black px-5 md:px-5 md:pt-9 pb-11 mt-32 grid gap-3',
    className,
  ]}
>
  <div
    class="flex items-start md:items-start justify-between flex-col md:flex-row gap-3 mb-2 sm:mb-0"
  >
    <div class="text-faded-black flex flex-col gap-15 w-full md:w-auto">
      <nav class="flex flex-col md:flex-row gap-1 md:-ml-10">
        <div class="flex md:justify-center justify-between border-b border-faded-black md:border-0 pb-3 pt-3 md:pt-0 md:pb-0 border-t">
          {
            navLinks.map((link) => (
              <a href={link.href} class={navLinkBase}>
                {link.label}{' '}
                {link.external ? <ArrowUpRight size="1em" /> : null}
              </a>
            ))
          }
        </div>
        <div class="flex gap-6 justify-start md:justify-center pt-5 pb-8  md:gap-0 md:py-0 md:pb-0 ">
          <a
            href={DISCORD_INVITE_URL}
            class={navLinkBase}
            aria-label="Discord"
            target="_blank"
            rel="noopener noreferrer"
          >
            <Discord size="1.5em" strokeWidth="0.0625em" />
          </a>
          <a
            href={GITHUB_REPO_URL}
            class={navLinkBase}
            aria-label="GitHub"
            target="_blank"
            rel="noopener noreferrer"
          >
            <GitHub size="1.5em" strokeWidth="0.0625em" />
          </a>
              <div class="block md:hidden ml-auto">
       <ThemeToggle client:visible />
    </div>
        </div>
      </nav>
    <div class="hidden md:block">
      <LogoMonoVersion width={"23"} />
    </div>
    </div>

    <div class="flex flex-col items-end text-end gap-15">
      <div class="hidden md:block -mr-5">
        <ThemeToggle client:visible />
      </div>
      <p class="text-sm text-left md:text-right text-faded-black font-sans">
        Video.js is a free and open source HTML5 <a href="/" class="underline"
          >video player.</a
        >
        <br />
        <a
          href="https://www.mux.com?utm_source=videojs&utm_campaign=vjs10"
          class="underline"
        >
          Video hosting and streaming sponsored by Mux
        </a>

        <br /><br />The term VIDEO.JS is a registered trademark of Brightcove
        Inc.
        <br />
        &copy; 2010&ndash;{new Date().getFullYear()} Video.js contributors
      </p>
    </div>
    <div class="block md:hidden pt-20">
      <LogoMonoVersion width={"20"} />
    </div>
  </div>
</footer>
