---
import Aside from '@/components/Aside.astro';
import JSPickerClient from './JSPickerClient';
import { INSTALLATION_ROUTES, isInstallationRouteSegment } from '@/utils/installation/routes';

const { framework } = Astro.params;
if (!isInstallationRouteSegment(framework)) throw new Error(`Unsupported installation route: ${framework}`);

const routeFramework = INSTALLATION_ROUTES[framework].pickerFramework;
---

<div>
  <JSPickerClient client:load currentFramework={routeFramework} route={framework}>
    {
      framework !== 'cdn' && (
        <Aside type="note" title="CDN is available for HTML">
          To load Video.js without a package manager, choose CDN in <a href="#choose-how-to-install">Choose how to install</a> below.
        </Aside>
      )
    }
  </JSPickerClient>
</div>
