---
import JSPickerClient from './JSPickerClient';
import { isInstallationRouteSegment } from '@/utils/installation/routes';

const { framework } = Astro.params;
if (!isInstallationRouteSegment(framework)) throw new Error(`Unsupported installation route: ${framework}`);

const routeFramework = framework === 'react' || framework === 'html' || framework === 'vue' || framework === 'svelte'
  ? framework
  : framework === 'cdn'
    ? 'html'
    : 'react';
---

<JSPickerClient client:idle currentFramework={routeFramework} route={framework} />
