---
title: 'Svelte Installation Guide'
description: 'Install Video.js with Svelte in Vite, Astro, or SvelteKit and build a player with HTML custom elements'
---

import JSPicker from '@/components/installation/JSPicker.astro';
import HTMLInstallTabs from '@/components/installation/HTMLInstallTabs.astro';
import InstallationAgentStart from '@/components/installation/InstallationAgentStart.astro';
import InstallationOptionsForAgents from '@/components/installation/InstallationOptionsForAgents.astro';
import InstallationMethodNav from '@/components/installation/InstallationMethodNav.astro';
import InstallationOverview from '@/components/installation/InstallationOverview.astro';
import InstallationPreview from '@/components/installation/InstallationPreview';
import MediaSourcePicker from '@/components/installation/MediaSourcePicker';
import ExtensionPicker from '@/components/installation/ExtensionPicker';
import SkinPickerSection from '@/components/installation/SkinPickerSection.astro';
import SvelteCreateCodeBlock from '@/components/installation/SvelteCreateCodeBlock';
import SvelteUsageCodeBlock from '@/components/installation/SvelteUsageCodeBlock';
import UseCasePicker from '@/components/installation/UseCasePicker';
import ContentWidth from '@/components/frames/ContentWidth.astro';
import Aside from '@/components/Aside.astro';
import DocsLink from '@/components/docs/DocsLink.astro';
import DocsLinkCard from '@/components/docs/DocsLinkCard.astro';
import DocsLinkCards from '@/components/docs/DocsLinkCards.astro';
import HumanCase from '@/components/docs/HumanCase.astro';
import Step from '@/components/docs/Step.astro';
import Steps from '@/components/docs/Steps.astro';
import RegistryOptions from '@/components/registry/RegistryOptions.astro';
import ProjectCommands from '@/components/installation/ProjectCommands';
import StartingPointPicker from '@/components/installation/StartingPointPicker';

<InstallationOverview />

## AI Quickstart

<InstallationAgentStart />

<InstallationOptionsForAgents />

<HumanCase>

## Choose your framework

Choose the JavaScript framework for your project.

<ContentWidth margins="lg">
  <JSPicker />
</ContentWidth>

## Choose your app setup

Choose whether the instructions target a Vite, Astro, or SvelteKit app.

<RegistryOptions framework="svelte" kind="template" method="packaged" />

## Choose your use case

Choose the ready-made player that is closest to what you are building.

<ContentWidth margins="lg">
  <UseCasePicker client:load />
</ContentWidth>

<ContentWidth margins="lg">
  <SkinPickerSection />
</ContentWidth>

<ContentWidth margins="lg">
  <InstallationPreview client:load />
</ContentWidth>

## Choose your media

Paste a media URL to detect its source type, pick one directly, or upload a video to Mux. The generated packages and component update with your choice.

<ContentWidth margins="lg">
  <MediaSourcePicker client:load />
</ContentWidth>

## Choose your extensions

Add optional behavior to the player. Only extensions that work with your player and media source appear below.

<ContentWidth margins="lg">
  <ExtensionPicker client:load />
</ContentWidth>

## Choose how to install

Choose from the installation options available for your selections.

<InstallationMethodNav />

</HumanCase>

<div data-installation-plan class="contents">

## Prepare your app

Choose whether to create the selected Svelte app setup or add Video.js to a project you already have.

<div data-llms-ignore>
<ContentWidth margins="lg">
  <StartingPointPicker client:load serverTemplate="vite" />
</ContentWidth>
</div>

<ProjectCommands client:load part="create" serverFramework="svelte" serverTemplate="vite" />

## Install the packages

Install the HTML elements and any playback adapter your selected media source needs.

<HTMLInstallTabs />

## Add your player

<Steps>
  <Step number={1} title="Add the player component">
    Add a component that imports the elements it renders. Svelte passes hyphenated tags through to the browser, so no compiler configuration is needed. The imports register the selected player, skin, media, and extensions; the markup composes them.

    <SvelteCreateCodeBlock client:load />
  </Step>
  <Step number={2} title="Render the player">
    Render the component in a page and pass it the media source you selected above.

    <SvelteUsageCodeBlock client:load />
  </Step>
</Steps>

## Run your app

Start the app and verify that the selected media plays.

<ProjectCommands client:load part="run" serverFramework="svelte" serverTemplate="vite" />

</div>

That's it! You now have a working Video.js player.

<Aside type="note" title="Server rendering with SvelteKit">
Keep the element imports static. SvelteKit renders the custom-element markup on the server, and the browser upgrades it when the page loads. Put code that needs the element or browser APIs in `onMount`. See <DocsLink slug="guides/svelte" anchor="use-sveltekit">Use SvelteKit</DocsLink> for the details.
</Aside>

## Choose what to do next

<DocsLinkCards label="Customize">
<DocsLinkCard slug="guides/svelte" description="Read player state in onMount, pass objects as properties, and configure SvelteKit">Use Video.js with Svelte and SvelteKit</DocsLinkCard>
<DocsLinkCard slug="guides/customize-skins" anchor="style-a-packaged-skin" description="Change colors, typography, sizing, and poster presentation">Customize skins</DocsLinkCard>
<DocsLinkCard slug="guides/architecture" description="Learn how the player, skins, media, and UI components fit together">Architecture</DocsLinkCard>
</DocsLinkCards>

<DocsLinkCards label="Deploy">
<DocsLinkCard slug="guides/browser-support" description="Check supported browsers and rendering environments">Browser support</DocsLinkCard>
<DocsLinkCard slug="guides/typescript" description="Configure TypeScript for Video.js packages">TypeScript</DocsLinkCard>
<DocsLinkCard slug="guides/bundlers" description="Check bundler compatibility">Bundlers</DocsLinkCard>
<DocsLinkCard slug="guides/content-security-policy" description="Configure Content Security Policy for your player">Content Security Policy</DocsLinkCard>
<DocsLinkCard slug="guides/cdn" description="See every bundle the CDN serves and the layout rules behind the URLs">CDN</DocsLinkCard>
<DocsLinkCard slug="guides/self-hosting" description="Serve the player from your own origin for offline or restricted-network deployments">Self-host the player</DocsLinkCard>
</DocsLinkCards>

Something not quite right? You can [submit an issue](https://github.com/videojs/v10/issues) and ask for help, or explore [other support options](/html5-video-support).
