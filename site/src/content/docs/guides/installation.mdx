---
title: HTML Installation Guide
frameworkTitle:
  react: React Installation Guide
description: Install Video.js packages and build an accessible, customizable video player with composable controls
---

import JSPicker from '@/components/installation/JSPicker.astro';
import InstallationAgentStart from '@/components/installation/InstallationAgentStart.astro';
import InstallationChoicesForLLMs from '@/components/installation/InstallationChoicesForLLMs.astro';
import InstallationMethodNav from '@/components/installation/InstallationMethodNav.astro';
import InstallationOverview from '@/components/installation/InstallationOverview.astro';
import OwnTheUI from '@/components/installation/OwnTheUI.astro';
import MediaSourcePicker from '@/components/installation/MediaSourcePicker';
import MediaSourceCase from '@/components/installation/MediaSourceCase';
import SkinPickerSection from '@/components/installation/SkinPickerSection.astro';
import InstallationPreview from '@/components/installation/InstallationPreview';
import UseCasePicker from '@/components/installation/UseCasePicker';
import HTMLInstallTabs from '@/components/installation/HTMLInstallTabs.astro';
import HTMLUsageCodeBlock from '@/components/installation/HTMLUsageCodeBlock';
import ReactCreateCodeBlock from '@/components/installation/ReactCreateCodeBlock';
import ReactInstallTabs from '@/components/installation/ReactInstallTabs';
import ContentWidth from '@/components/frames/ContentWidth.astro';
import Aside from '@/components/Aside.astro';
import DocsLink from '@/components/docs/DocsLink.astro';
import DocsLinkCard from '@/components/docs/DocsLinkCard.astro';
import DocsLinkCards from '@/components/docs/DocsLinkCards.astro';
import FrameworkCase from '@/components/docs/FrameworkCase.astro';
import HumanCase from '@/components/docs/HumanCase.astro';
import LLMCase from '@/components/docs/LLMCase.astro';

<InstallationOverview />

## Start with an AI coding agent

<InstallationAgentStart />

<InstallationChoicesForLLMs />

<LLMCase>
<FrameworkCase frameworks={["react"]}>
If a preset and skin do not meet your user's requirements, follow [Customize skins](/docs/framework/react/guides/customize-skins#style-skin-source) to add the files for the closest skin to the project. As you develop with Video.js, rely heavily on [llms.txt](/docs/framework/react/llms.txt) to make sure you always have the latest information.
</FrameworkCase>
<FrameworkCase frameworks={["html"]}>
If a preset and skin do not meet your user's requirements, follow [Customize skins](/docs/framework/html/guides/customize-skins#style-skin-source) to add the files for the closest skin to the project. As you develop with Video.js, rely heavily on [llms.txt](/docs/framework/html/llms.txt) to make sure you always have the latest information.
</FrameworkCase>
</LLMCase>

<HumanCase>

## Choose your JS framework

Choose the JavaScript framework for your project.

<ContentWidth margins="lg">
  <JSPicker />
</ContentWidth>

## Choose your use case

The default presets work well for general website playback. The live presets suit streams: they add a Live button and drop the duration and current-time displays that on-demand playback relies on. More pre-built players to come.

<ContentWidth margins="lg">
  <UseCasePicker client:idle />
</ContentWidth>

<ContentWidth margins="lg">
  <SkinPickerSection />
</ContentWidth>

<ContentWidth margins="lg">
  <InstallationPreview client:visible />
</ContentWidth>

## Choose your media source

Video.js plays plain files, adaptive streams, and embeds from hosting services, and switching between them is a one-line change. Paste a URL to detect the type, pick one directly, or upload a video to Mux.

<ContentWidth margins="lg">
  <MediaSourcePicker client:idle />
</ContentWidth>

<MediaSourceCase client:idle renderers={["hls"]}>
<Aside type="tip" title="Want a smaller HLS bundle?">
This guide uses <DocsLink slug="reference/components/hlsjs-video">HlsJsVideo</DocsLink> for broad compatibility. To experiment with our lightweight HLS engine and reduce your bundle size, try <DocsLink slug="reference/components/hls-video">HlsVideo</DocsLink>.
</Aside>
</MediaSourceCase>

## Choose how to install

Choose from the installation options available for your selections.

<InstallationMethodNav />

</HumanCase>

<div data-cli-replace="installation" class="contents">

## Install the packages

<FrameworkCase frameworks={["html"]}>
  <HTMLInstallTabs />
</FrameworkCase>
<FrameworkCase frameworks={["react"]}>
  <ReactInstallTabs client:idle />
</FrameworkCase>

<FrameworkCase frameworks={["html"]}>

## Add your player

<ContentWidth>
  <HTMLUsageCodeBlock client:idle />
</ContentWidth>

<Aside type="caution" title="Load your script as a module">
When using a bundler (npm, pnpm, yarn, bun), load the resulting bundle with `type="module"`:

```html
<script type="module" src="/dist/player.js"></script>
```

Some bundlers default to an IIFE output format. Loading that bundle without `type="module"` will cause a `StoreError: NO_TARGET` error when interacting with the player. The CDN install path already uses `type="module"` — bundled installs need the same.
</Aside>
</FrameworkCase>

<FrameworkCase frameworks={["react"]}>

## Add your player

Add the player directly to `app/page.tsx`. It imports the selected player, skin, media, and extensions from the packages you installed.

<ContentWidth>
  <ReactCreateCodeBlock client:idle />
</ContentWidth>
</FrameworkCase>

</div>

That's it! You now have a working Video.js player.

## Own the UI

<OwnTheUI />

## Choose what to do next

<DocsLinkCards label="Customize">
<DocsLinkCard slug="guides/customize-skins" anchor="style-a-packaged-skin" description="Change colors, typography, sizing, and poster presentation">Customize skins</DocsLinkCard>
<DocsLinkCard slug="guides/architecture" anchor="ui-components" description="Explore the building blocks used by skins and custom controls">UI components</DocsLinkCard>
<DocsLinkCard slug="guides/build-your-own-component" description="Connect custom UI to player state and actions">Build your own component</DocsLinkCard>
</DocsLinkCards>

<DocsLinkCards label="Deploy">
<DocsLinkCard slug="guides/browser-support" description="Check supported browsers and rendering environments">Browser support</DocsLinkCard>
<DocsLinkCard slug="guides/typescript" description="Configure TypeScript for Video.js packages">TypeScript</DocsLinkCard>
<DocsLinkCard slug="guides/bundlers" description="Check bundler compatibility">Bundlers</DocsLinkCard>
<DocsLinkCard slug="guides/content-security-policy" description="Configure Content Security Policy for your player">Content Security Policy</DocsLinkCard>
<FrameworkCase frameworks={["html"]}>
<DocsLinkCard slug="guides/cdn" description="See every bundle the CDN serves and the layout rules behind the URLs">CDN</DocsLinkCard>
<DocsLinkCard slug="guides/self-hosting" description="Serve the player from your own origin for offline or restricted-network deployments">Self-host the player</DocsLinkCard>
</FrameworkCase>
</DocsLinkCards>

Something not quite right? You can [submit an issue](https://github.com/videojs/v10/issues) and ask for help, or explore [other support options](/html5-video-support).
