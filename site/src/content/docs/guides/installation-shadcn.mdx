---
title: Shadcn Installation Guide
description: Use the Video.js Shadcn registry to add editable React or HTML custom-element skin source to your project
---

import Aside from '@/components/Aside.astro';
import DocsLink from '@/components/docs/DocsLink.astro';
import HumanCase from '@/components/docs/HumanCase.astro';
import Step from '@/components/docs/Step.astro';
import Steps from '@/components/docs/Steps.astro';
import ContentWidth from '@/components/frames/ContentWidth.astro';
import InstallationChoicesForLLMs from '@/components/installation/InstallationChoicesForLLMs.astro';
import JSPicker from '@/components/installation/JSPicker.astro';
import InstallationMethodNav from '@/components/installation/InstallationMethodNav.astro';
import InstallationOverview from '@/components/installation/InstallationOverview.astro';
import InstallationPreview from '@/components/installation/InstallationPreview';
import MediaSourcePicker from '@/components/installation/MediaSourcePicker';
import ShadcnNextSteps from '@/components/installation/ShadcnNextSteps.astro';
import SkinPickerSection from '@/components/installation/SkinPickerSection.astro';
import UseCasePicker from '@/components/installation/UseCasePicker';
import RegistryOptions from '@/components/registry/RegistryOptions.astro';
import RegistryInitCommand from '@/components/registry/RegistryInitCommand.astro';
import InstallationRegistryCommand from '@/components/registry/InstallationRegistryCommand.astro';
import ShadcnFrameworkCase from '@/components/registry/ShadcnFrameworkCase.astro';
import SourceMediaInstall from '@/components/registry/SourceMediaInstall';
import SourceReactPlayer from '@/components/registry/SourceReactPlayer';
import SourceHTMLPlayer from '@/components/registry/SourceHTMLPlayer';

<InstallationOverview />

<InstallationChoicesForLLMs />

<HumanCase>

## Choose your JS framework

Choose the JavaScript framework for your project.

<ContentWidth margins="lg">
  <JSPicker />
</ContentWidth>

## Choose your use case

Choose the ready-made player that is closest to what you are building. The Shadcn registry does not currently provide the background video skin as source.

<ContentWidth margins="lg">
  <UseCasePicker client:idle includeBackground={false} />
</ContentWidth>

<ContentWidth margins="lg">
  <SkinPickerSection includeNoSkin={false} source />
</ContentWidth>

<ContentWidth margins="lg">
  <InstallationPreview client:visible />
</ContentWidth>

## Choose your media source

Choose what the player will play. If that source needs a playback adapter, the installation steps include it.

<ContentWidth margins="lg">
  <MediaSourcePicker client:idle />
</ContentWidth>

## Choose how to install

Choose from the installation options available for your selections.

<InstallationMethodNav />

</HumanCase>

<div data-cli-replace="installation" class="contents">

## Initialize Shadcn

Run the commands from the app directory that contains `components.json`, or where you want the CLI to create it. In a monorepo, run them from the app workspace or pass `--cwd <path>`.

<Aside type="note">
Commit your work before installing or updating source so you can review every file the CLI adds or replaces.
</Aside>

<ShadcnFrameworkCase framework="react">
<RegistryOptions framework="react" kind="template" />

<RegistryInitCommand framework="react" installation />
</ShadcnFrameworkCase>

<ShadcnFrameworkCase framework="html">
<RegistryOptions framework="html" kind="template" />

<RegistryInitCommand framework="html" installation />
</ShadcnFrameworkCase>

If the app already has `components.json`, skip this section.

## Add the skin source

React skins can use Tailwind CSS 4 or Vanilla CSS. HTML skins use Vanilla CSS. Choose the styling that matches your app, then connect the Video.js registry and add the skin selected above.

<ShadcnFrameworkCase framework="react">
<RegistryOptions framework="react" kind="styling" />

<InstallationRegistryCommand framework="react" />

The files are added under `components/videojs/<preset>`. The registry item also installs `@videojs/react`.
</ShadcnFrameworkCase>

<ShadcnFrameworkCase framework="html">
<RegistryOptions framework="html" kind="styling" />

<InstallationRegistryCommand framework="html" />

The files are added under `components/videojs/<preset>`. The registry item also installs `@videojs/html`.
</ShadcnFrameworkCase>

<SourceMediaInstall client:idle />

## Add your player

<ShadcnFrameworkCase framework="react">
Add the player directly to `app/page.tsx`. It imports the player and media from the package, then imports the selected skin from your project.

<ContentWidth>
  <SourceReactPlayer client:idle />
</ContentWidth>
</ShadcnFrameworkCase>

<ShadcnFrameworkCase framework="html">
The HTML source also works in Vue and Svelte. Follow the custom-element setup in the <DocsLink slug="guides/installation-vue">Vue guide</DocsLink> or <DocsLink slug="guides/installation-svelte">Svelte guide</DocsLink>. In your component, import the installed `skin.ts` instead of a packaged skin, and place the updated `skin.html` markup inside the player. In Vue, include the tags from that markup in `isCustomElement`. The steps below show the plain HTML entry point.

<Steps>
  <Step number={1} title="Add the media element">
    <SourceHTMLPlayer client:idle part="media" />
  </Step>
  <Step number={2} title="Register the custom elements">
    <SourceHTMLPlayer client:idle part="imports" />
  </Step>
  <Step number={3} title="Add the player to your page">
    <SourceHTMLPlayer client:idle part="player" />
  </Step>
</Steps>
</ShadcnFrameworkCase>

</div>

That's it! You now have a working Video.js player whose skin source lives in your project.

## Change the skin source

<ShadcnFrameworkCase framework="react">
Start in `components/videojs/<preset>/skin.tsx`. Follow the [Customize skins](/docs/framework/react/guides/customize-skins#style-skin-source) guide for the files that control layout, colors, controls, and interactions.
</ShadcnFrameworkCase>

<ShadcnFrameworkCase framework="html">
Start in `components/videojs/<preset>/skin.html`. Follow the [Customize skins](/docs/framework/html/guides/customize-skins#style-skin-source) guide for the files that control layout, colors, controls, and interactions.
</ShadcnFrameworkCase>

## Choose what to do next

<ShadcnFrameworkCase framework="react">
<ShadcnNextSteps framework="react" />
</ShadcnFrameworkCase>

<ShadcnFrameworkCase framework="html">
<ShadcnNextSteps framework="html" />
</ShadcnFrameworkCase>
