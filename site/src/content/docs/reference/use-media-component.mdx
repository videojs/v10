---
title: useMediaComponent
description: Hook for registering a custom extension with the current Player media host
---

import UtilReference from "@/components/docs/api-reference/UtilReference.astro";
import DocsLink from "@/components/docs/DocsLink.astro";

## Import

```tsx
import { useMediaComponent } from "@videojs/react";
```

## Usage

Build a React <DocsLink slug="concepts/overview">extension</DocsLink> by passing its media component class from a component rendered inside `Player`.

```tsx
import { MuxDataExtension } from "@videojs/mux-data";
import { useMediaComponent } from "@videojs/react";

function CustomMuxData() {
  useMediaComponent(MuxDataExtension);

  return null;
}
```

The hook creates one instance, follows media changes, and destroys the component on unmount. A plain `<video>` element is not a media-component host, so registration is skipped for it.

<UtilReference util="useMediaComponent" />
