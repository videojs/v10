---
title: BackgroundVideoSkin
frameworkTitle:
  html: background-video-skin
description: The background preset's chrome-less skin for ambient video
---

import DocsLink from '@/components/docs/DocsLink.astro';
import FrameworkCase from '@/components/docs/FrameworkCase.astro';
import SkinImports from '@/components/docs/api-reference/SkinImports.astro';

The only skin of the background preset: a chrome-less surface that sizes ambient video with no visible controls. See <DocsLink slug="guides/skins">Skins</DocsLink> for how packaged skins work and <DocsLink slug="guides/presets">Presets</DocsLink> for what the background preset ships.

## Import

<SkinImports preset="background" react="BackgroundVideoSkin" />

## Usage

The skin goes between the player and the media element. It sizes the ambient video and renders no visible controls.

<FrameworkCase frameworks={["react"]}>
```tsx
<BackgroundVideoPlayer>
  <BackgroundVideoSkin>
    <BackgroundVideo src="video.mp4" />
  </BackgroundVideoSkin>
</BackgroundVideoPlayer>
```
</FrameworkCase>

<FrameworkCase frameworks={["html"]}>
```html
<background-video-player>
  <background-video-skin>
    <background-video src="video.mp4"></background-video>
  </background-video-skin>
</background-video-player>
```
</FrameworkCase>

## Styling

Packaged skins style themselves and expose a small set of public CSS custom properties. Everything else inside the skin is private and may change between releases. <DocsLink slug="guides/customize-skins" anchor="style-a-packaged-skin">Customize skins</DocsLink> documents the properties.

## Own the UI

Owning the UI means keeping its components, layout, styles, and interactions as local files you can change. The <DocsLink slug="guides/installation-shadcn">Shadcn installation guide</DocsLink> adds those files for supported skins. The registry does not provide the background video skin as source, so build an equivalent from the source components by following <DocsLink slug="guides/build-your-own-component">Build your own UI component</DocsLink>.
