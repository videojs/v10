---
import DialNav from '@/components/DialNav';
import Footer from '@/components/Footer.astro';
import FooterEasterEgg from '@/components/FooterEasterEgg.astro';
import NavBar from '@/components/NavBar/NavBar.astro';
import Base from '@/layouts/Base.astro';

interface Props {
  title: string;
  description: string;
  code?: string;
  message?: string;
}

const { title, description, code = '404', message = 'NOT FOUND' } = Astro.props;
---

<Base {title} {description}>
  <div class="flex min-h-screen flex-col">
    <NavBar />
    <main class="w-full">
      <div
        class="mx-auto grid w-full max-w-295 justify-between gap-8 px-5 py-10 md:flex md:pt-32 md:pb-16"
      >
        <div class="flex flex-col leading-none">
          {/* TODO(old-type): text-display-h2, text-display-h1 */}
          <h1
            class="text-display-h2 md:text-display-h1 font-display font-bold text-faded-black dark:text-manila-light"
          >
            {code}
          </h1>
          {/* TODO(old-type): text-display-h2, text-display-h1 */}
          <h2
            class="text-display-h2 md:text-display-h1 font-display font-bold text-stroke-faded-black dark:text-stroke-manila-light"
          >
            {message}
          </h2>
        </div>
        <DialNav
          client:load
          left={[
            { href: "/", label: "HOME", angle: -70 },
            { href: "/docs", label: "DOCS", angle: -110 },
          ]}
          right={[
            { href: "https://mux.com", label: "MUX.COM", angle: 70 },
            { href: "https://mux.com/blog", label: "BLOG", angle: 110 },
          ]}
        />
      </div>
    </main>
    <Footer />
  </div>
  <FooterEasterEgg />
</Base>
