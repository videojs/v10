/** localStorage key for the persisted color-theme preference. */
export const THEME_KEY = 'vjs-api-demo-theme';

/** `theme-color` meta values, matched to the demo's light/dark page backgrounds. */
export const THEME_COLOR = {
  light: '#f3e7d2',
  dark: '#1e1d1d',
} as const;
